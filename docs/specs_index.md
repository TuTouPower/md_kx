# 当前生效 spec 清单

在表即生效。每个 task 收尾时写入或更新对应行（须已过黑盒）；废弃时整行删除，spec 移入 `docs/archive/specs/`。历史清单由 `docs/archive/specs/` 目录承载。替代旧需求时在 `supersedes` 列填旧 slug；无则填 `-`。

|slug|task 清单|最后更新时间|
|------|------|------|
|front_matter_preserve|t001|2026-08-11|
|unified_indent_width|t002|2026-08-11|
|builtin_table_handling|t008（supersedes t003）|2026-08-13|
