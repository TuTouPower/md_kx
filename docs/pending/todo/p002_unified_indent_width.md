# p002 支持统一缩进宽度

- 来源：用户提出（fork 二次开发需求）
- 内容：mdformat 遵循 CommonMark「marker 对齐内容」语义，不强制统一缩进，同一文档内 2/3/4/5/6 空格混用的嵌套列表缩进被判定全部合法后不改动，导致文档缩进风格混乱。期望：提供缩进宽度配置项（默认值不影响现有行为），让嵌套列表缩进统一为指定宽度（本项目统一 4 空格）。验收标准：①新增配置项可指定统一缩进宽度；②设置后文档内所有嵌套列表缩进统一为该宽度，无 2/3/5/6 等混用残留；③代码块（``` 围栏内）内容不被改动。
- 处理：未开
