# p004 内置表格解析与三态表格处理

- 来源：用户提出（fork 二次开发需求）
- 内容：mdformat 核心内置表格解析能力（不依赖 GFM 扩展），提供开关让用户三选一：①不处理表格（默认，表格内容不触碰）；②处理表格并 pad 对齐（单元格补空格对齐）；③处理表格并保持紧凑（单元格不补空格对齐，如 `| a | b |` 不变 `| a    | b   |`）。表格本身语法正确，能被正常渲染。
- 处理：未开
